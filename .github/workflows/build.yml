on:
  pull_request:
    paths: [package-lock.json]

permissions:
  contents: read

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - run: npm ci
      - run: npm run build
